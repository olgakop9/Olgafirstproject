//сумма элементов массива

int [] array = {1,2,3,4,5,6,7};
int n = array.Length;
int x = 0;
int sum = 0;
int rezult = 0;
 

 for (int i = 1; i <= n; i++)
 {
    sum = +x;
    rezult = sum / n;
      }
      {
            Console.WriteLine(rezult);
      }

           
